-- write a query that uses a subquery
-- we have a student table, degree table, classes, professors, enrollment, etc
-- List all the professors who teach in a room '101'


SELECT PROFESSOR.PROF_ID, PROFESSOR.PERSON_ID, PROFESSOR.DEPT_ID, PROFESSOR.OFFICE_NUM, PROFESSOR.BUILDING_ID